[defaults]
inventory = hosts.ini
roles_path = ./roles
remote_user = gaia
private_key_file = ~/.ssh/id_rsa
host_key_checking = False

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
